import { OperationsAvailableGuard } from './operations-available.guard';

describe('OperationsAvailableGuard', () => {
  it('should be defined', () => {
    expect(new OperationsAvailableGuard()).toBeDefined();
  });
});
